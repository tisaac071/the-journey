# the-journey

This repository contains a lightweight, responsive one-page portfolio for "Isaac Tumusiime" with an updated classic-modern interface.

Preview locally:

```bash
# from the repository root
python3 -m http.server 8000 --directory .
# then open http://127.0.0.1:8000 in your browser
```

Files added:

- `index.html` — main one-page portfolio
- `styles.css` — site styles and responsive layout
- `script.js` — small UI helpers (mobile nav, smooth scroll)

Next steps: customize copy, replace the placeholder contact email (updated to `tisaac071@gmail.com`), add a professional photo, and link to LinkedIn. Project link added: https://caregiversuganda.co.ug

Profile photo: saved locally as `profile.jpg` and displayed from the site root. If the image still does not appear, ensure the browser is loading the local file from the same directory.
